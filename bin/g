#!/usr/bin/env bash


if git rev-parse --is-inside-work-tree &>/dev/null || [[ "$1" == "init" ]]; then
    git "$@"
elif command -v tree &>/dev/null; then
    tree -aL 1 --noreport
else
    ls -AF
fi
