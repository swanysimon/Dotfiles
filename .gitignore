# configurations generally shouldn't be copied between machines, either for
# security reasons or because I just don't understand what's involved
/config/*
!/config/bash/*
/config/bash/local_config/*
/config/bash/sourcing/*
!/config/bin/*
!/config/git/*

# gitkeep files exist to preserve directory structure, even when the underlying
# directory contains things that shouldn't be checked in
!.gitkeep

# things from vim that shouldn't be checked in
/vim/.caches/*/*
/vim/.netrwhist
