# configs should generally not be checked in, but some are worth monitoring
/config/*
!/config/git
!/config/nvim

# shell/hidden is where system-specific sourcing can be placed
/shell/hidden/*

# gitkeep files exist to preserve directory structure, even when the underlying
# directory contains things that shouldn't be checked in
!.gitkeep
